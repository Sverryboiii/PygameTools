[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sverpykit"
version = "0.2.3"
description = "A wrapper for pygame."
readme = "README.md"
dependencies = [
    "pygame"
]
