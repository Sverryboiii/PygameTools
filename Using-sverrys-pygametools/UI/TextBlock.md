# TextBlock

A textblock is a simple way to display text in without having to keep track yourself.

Parameters:<br>
`rect`: Only the first 2 digits mater for the position. The size is irralivant.<br>
`text`: The text input (Type-hints state that a String object must be used, but as long as it can be converted to string it's allowed).