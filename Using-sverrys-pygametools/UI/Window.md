# Window

A window is great for organizing all your UI.

Parameters:<br>
`owner`: The list that the window is located in.<br>
`rect`: The position and size of the rectangle.<br>
`components`: All the UI that belongs to the window.<br>
`color`: The color of the window.<br>
`title_bar`: If the window has a title bar (Doesn't work yet).